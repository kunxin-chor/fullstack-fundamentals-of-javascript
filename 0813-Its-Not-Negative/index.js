const prompt = require("prompt-sync")();

let numbers = [];
let n = 0;

// keep asking for numbers till the user type in something negative
while (`???`) {
  n = parseInt(prompt());
  if (`???`) {

  }
}

// print what the user has typed in
`for ???`;







// To test your code, npm install, and in CLI type node index.js and use test cases in readme;
