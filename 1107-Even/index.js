function isEven(x) {
  `???`
}

function printEven(f) {

  `???`

  for (i = 1; i <= 20; i++) {
    if (`???`) {
      console.log(i);
      `???`
    }
  }
  return `???`;
}
           
printEven(`???`);


module.exports = { isEven, printEven }

// To test your code
// in CLI type, npm install
// when that is done, in CLI type, npm run test;
