const prompt = require("prompt-sync")();

let userString = prompt("Give me a string: ");

console.log(userString.length);
console.log(userString[userString.length-1]);














// node index.js to see what is printed matches.

