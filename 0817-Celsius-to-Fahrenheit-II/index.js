const prompt = require("prompt-sync")();

let temperature;
let temperatureHolder = [];














// To test your code
// in CLI type, npm install
// when that is done, in CLI type node index.js and compare w test case in readme;
