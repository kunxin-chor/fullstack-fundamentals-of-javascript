const prompt = require("prompt-sync")();

let s = "";
let t = "";

for (i = 0; i < 3; i++) {
  s += "";
  t = "";
    for (j = 0; j < 3; j++) {
      t += "*";
    }
  console.log(t);
}




// To test your code, in CLI type npm install, when that is complete, type node index.js to see if it matches what is in readme;
