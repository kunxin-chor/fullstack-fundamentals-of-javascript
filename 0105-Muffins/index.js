// type codes here //













// Do not touch below //

module.exports = {
    d,
    c,
    n,
    total
}

// To test your code
// in CLI type, npm install
// when that is done, in CLI type, npm run test;
