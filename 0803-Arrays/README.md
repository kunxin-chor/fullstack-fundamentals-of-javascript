Define an array, named **myArray**, to have the following elements, in order:

- A string "You are okay"
- The integer is 8123
- The float 20.20
- The boolean true

Print out the array using console.log.