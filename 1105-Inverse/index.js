function inverse(x) {
  return x * -1;
}

function foobar(f, x) {
  return f(x);
}

x = foobar(`???`, 3);
console.log(`???`);

// To test your code
// in CLI type, node index.js and check against readme;
